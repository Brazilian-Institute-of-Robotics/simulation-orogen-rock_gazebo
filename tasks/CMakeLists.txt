# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gazeboTaskLib)
ADD_LIBRARY(${GAZEBO_TASKLIB_NAME} SHARED 
    ${GAZEBO_TASKLIB_SOURCES})
add_dependencies(${GAZEBO_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${GAZEBO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GAZEBO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GAZEBO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GAZEBO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GAZEBO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GAZEBO_TASKLIB_HEADERS}
    DESTINATION include/orocos/gazebo)

